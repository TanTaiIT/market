node_modules/
dist/
# Mọi file env đều chứa MONGO_URI (kèm mật khẩu cluster) hoặc JWT secret — chỉ `*.example`
# được commit. Pattern phải phủ cả `.env.development` / `.env.production` / `.env.bak`.
.env
.env.*
!.env.example
!.env.*.example
logs/
*.log
coverage/
# Dump dữ liệu thật lấy từ Atlas trước khi seed — chứa email, hash mật khẩu, nội dung chat.
backup/
.DS_Store
.idea/
.vscode/
